# Core
flask>=3.0.0
flask-socketio>=5.3.6
flask-cors>=4.0.0
python-dotenv>=1.0.0

# MAVSDK
mavsdk>=1.4.0

# YOLO - use ONE of:
onnxruntime>=1.16.0          # ONNX (recommended for RPi5, no GPU needed)
# ultralytics>=8.0.0         # PyTorch fallback (heavier)

# GStreamer Python bindings (install via apt on RPi, NOT pip)
# sudo apt install python3-gi python3-gi-cairo gir1.2-gst-1.0 \
#   gstreamer1.0-tools gstreamer1.0-plugins-base gstreamer1.0-plugins-good \
#   gstreamer1.0-plugins-bad gstreamer1.0-v4l2 gstreamer1.0-libav

# NumPy (required by YOLO consumer)
numpy>=1.24.0

# Optional: for OpenCV (only used in ONNX preprocessing fallback)
opencv-python-headless>=4.8.0
